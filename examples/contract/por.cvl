#include <civlc.cvh>
#include <memory.cvh>

/*@ depends \anyact;
  @ assigns a, b;
  @*/
void test(int a, int b);

/*@ depends (\write(n)+\read(n))-(\call(inc, n)+\call(dec, n));
  @*/
void foo(int n);
